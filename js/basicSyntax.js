var s = 100; // 定义变量
var name; // 没有赋值，默认的值是undifined

"hello world!" // 字符串字面量
1234 ; // 数字字面量

var Location ="成都"
var location = "chengdu" // 大小写敏感

/*空值, null, undefined的比较*/
var person;           // 值是 undefined，类型是 undefined
person = undefined;
var car = "";         // 值是 ""，类型是 "string"
var name = null;           // 值是 null，但是类型仍然是对象

var length = 7;                             // 数字
var lastName = "Gates";                      // 字符串
var cars = ["Porsche", "Volvo", "BMW"];         // 数组
var x = {firstName:"Bill", lastName:"Gates"};    // 对象
